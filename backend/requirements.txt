# --- Core API Framework and Server ---
uvicorn==0.22.0
axios
# --- HTTP Clients ---
requests==2.32.4  # Uncomment ONLY if you use requests in your codebase

# --- Environment Variables ---
python-dotenv==1.0.0

# --- Database & Supabase Client ---
supabase==0.2.0

# --- Security Vulnerability Patches (Pinned by Snyk) ---
urllib3==2.5.0      # Direct pin for security
websockets==10.0.0  # Direct pin for security

# --- Flask Stack (Remove if not using Flask anywhere) ---
Flask==2.2.5
Flask-CORS==6.0.0
Flask-Migrate

werkzeug>=3.0.6 # not directly required, pinned by Snyk to avoid a vulnerability
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
