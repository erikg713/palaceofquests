# ==============================
# Palace of Quests — Backend Requirements
# Flask-based backend with Supabase integration
# ==============================

Flask==2.2.5
Flask-CORS==6.0.0
Flask-Migrate==4.0.4
supabase==0.2.0
python-dotenv==1.0.0
requests==2.32.4
marshmallow==4.0.0

# Security-related pins (keep updated and audit regularly)
urllib3==2.5.0
websockets==9.1
werkzeug==3.0.6
zipp==3.19.1

# ==============================
# Notes:
# - All dependency versions are pinned for security and reproducibility.
# - Only include what you use—remove unnecessary packages.
# - Audit dependencies with pip-audit or similar tools.
# - For Pi Network SDK: install separately if needed.
# - Frontend JS deps go in package.json, not here.
# ==============================
